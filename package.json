{
  "name": "regravity-trade-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build && npm run post-build",
    "post-build": "node scripts/post-build.js",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview --host",
    "clean": "rm -rf dist",
    "deploy": "npm run clean && npm run build && echo '✅ Build complete! Upload the dist/ folder to your hosting platform.'",
    "deploy:netlify": "npm run deploy && echo '🚀 For Netlify: The _redirects and netlify.toml files are included.'",
    "deploy:vercel": "npm run deploy && echo '🚀 For Vercel: The vercel.json file is included.'",
    "deploy:github": "npm run deploy && echo '🚀 For GitHub Pages: Deploy the dist/ folder to gh-pages branch.'"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.8.1",
    "react-hook-form": "^7.48.2",
    "react-icons": "^4.12.0",
    "@supabase/supabase-js": "^2.39.0",
    "@emailjs/browser": "^3.11.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@vitejs/plugin-react": "^4.2.1",
    "eslint": "^8.55.0",
    "eslint-plugin-react": "^7.33.2",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.5",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.3.6",
    "terser": "^5.24.0",
    "vite": "^5.0.8"
  }
}